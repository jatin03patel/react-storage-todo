# Todo App with Custom useStorage Hook

This is a React Todo app demonstrating a custom hook (`useStorage`) for managing **localStorage** and **sessionStorage**.

## Features
- Add todos to Local and Session storage
- Clear buttons for each storage type
- Professional gradient layout
- Responsive design
